services:
  - type: web
    name: seasonality-cot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 -m streamlit run dashboard.py --server.port $PORT --server.address 0.0.0.0
    autoDeploy: true
